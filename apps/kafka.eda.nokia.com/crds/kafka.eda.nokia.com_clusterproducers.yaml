---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: clusterproducers.kafka.eda.nokia.com
spec:
  group: kafka.eda.nokia.com
  names:
    kind: ClusterProducer
    listKind: ClusterProducerList
    plural: clusterproducers
    singular: clusterproducer
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ClusterProducer is the Schema for the clusterproducers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterProducer
            properties:
              brokers:
                description: Comma separated Kafka brokers list
                minLength: 1
                type: string
              compressionCodec:
                default: none
                description: Data compression codec
                enum:
                - none
                - gzip
                - snappy
                - zstd
                - lz4
                type: string
              exports:
                description: Exports is the list of paths to be exported as well as
                  the topic and mode for each.
                items:
                  description: Export defines an object to be exported and
                  properties:
                    fields:
                      description: list of fields to be exported from the configured
                        EDB table.
                      items:
                        type: string
                      type: array
                    mode:
                      default: onChange
                      description: Export Mode
                      enum:
                      - onChange
                      - periodic
                      - periodicOnChange
                      type: string
                    path:
                      description: The EDB path to export, in the format '.namespace.node.srl.interface'
                      type: string
                    period:
                      default: 10s
                      description: Export Period. A 1s minimum is enforced.
                      type: string
                    topic:
                      description: |-
                        A topic name to which the producer must write
                        updates from this export path
                      type: string
                    where:
                      description: A where clause to use for the query, e.g. 'oper-state
                        = down'. You can omit enclosing parentheses.
                      type: string
                  required:
                  - path
                  - topic
                  type: object
                type: array
              flushFrequency:
                description: The best-effort frequency of flushes
                type: string
              maxRetry:
                default: 3
                description: The total number of times to retry sending a message
                type: integer
              requiredAcks:
                default: noResponse
                description: Number of replica acknowledgements the broker must see
                  before responding
                enum:
                - noResponse
                - waitForLocal
                - waitForAll
                type: string
              sasl:
                description: Kafka SASL configuration
                properties:
                  mechanism:
                    default: plain
                    description: SASL Mechanism
                    enum:
                    - plain
                    - scramSha256
                    - scramSha512
                    - oauthBearer
                    type: string
                  password:
                    description: SASL password
                    type: string
                  tokenURL:
                    description: token url for OAUTHBEARER SASL mechanism
                    type: string
                  user:
                    description: SASL user name
                    type: string
                type: object
              syncProducer:
                description: Start a sync producer
                type: boolean
              timeout:
                default: 10s
                description: Kafka producer timeout
                type: string
              tls:
                description: Kafka TLS configuration
                properties:
                  fromFiles:
                    description: Certificates files.
                    properties:
                      caFile:
                        description: Path to a certificate authority file.
                        type: string
                      certFile:
                        description: The client certificate file location.
                        type: string
                      keyFile:
                        description: The client private key location.
                        type: string
                      skipVerify:
                        description: If true the client will not verify the server's
                          certificate.
                        type: boolean
                    type: object
                  fromSecret:
                    description: |-
                      Secret containing a `tls.crt`, a `tls.key` and a `ca.crt` keys.
                      Both `tls.crt` and `tls.key` must be present.
                      If `ca.crt` is not present and `.trustBundle` is not set
                      the remote server certificate is not verified.
                    type: string
                  trustBundle:
                    description: |-
                      ConfigMap containing a set of trust bundles (key `trust-bundle.pem`) used to
                      verify the remote server certificates.
                    type: string
                type: object
            required:
            - brokers
            - exports
            type: object
          status:
            description: status defines the observed state of ClusterProducer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - deprecated: true
    deprecationWarning: This version is deprecated and will no longer be supported
      in the next version.
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterProducer is the Schema for the clusterproducers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterProducerSpec defines the desired state of ClusterProducer
            properties:
              brokers:
                description: Comma separated Kafka brokers list
                minLength: 1
                type: string
              compression-codec:
                default: none
                description: Data compression codec
                enum:
                - none
                - gzip
                - snappy
                - zstd
                - lz4
                type: string
              exports:
                description: Exports is the list of paths to be exported as well as
                  the topic and mode for each.
                items:
                  description: Export defines an object to be exported and
                  properties:
                    fields:
                      description: list of fields to be exported from the configured
                        EDB table.
                      items:
                        type: string
                      type: array
                    mode:
                      default: on-change
                      description: Export Mode
                      enum:
                      - on-change
                      - periodic
                      - periodic-on-change
                      type: string
                    path:
                      description: The EDB path to export, in the format '.namespace.node.srl.interface'
                      type: string
                    period:
                      default: 10s
                      description: Export Period. A 1s minimum is enforced.
                      type: string
                    topic:
                      description: |-
                        A topic name to which the producer must write
                        updates from this export path
                      type: string
                    where:
                      description: A where clause to use for the query, e.g. 'oper-state
                        = down'. You can omit enclosing parentheses.
                      type: string
                  required:
                  - path
                  - topic
                  type: object
                type: array
              flush-frequency:
                description: The best-effort frequency of flushes
                type: string
              max-retry:
                default: 3
                description: The total number of times to retry sending a message
                type: integer
              required-acks:
                default: no-response
                description: Number of replica acknowledgements the broker must see
                  before responding
                enum:
                - no-response
                - wait-for-local
                - wait-for-all
                type: string
              sasl:
                description: Kafka SASL configuration
                properties:
                  mechanism:
                    default: plain
                    description: SASL Mechanism
                    enum:
                    - plain
                    - scram-sha-256
                    - scram-sha-512
                    - oauthbearer
                    type: string
                  password:
                    description: SASL password
                    type: string
                  token-url:
                    description: token url for OAUTHBEARER SASL mechanism
                    type: string
                  user:
                    description: SASL user name
                    type: string
                type: object
              sync-producer:
                description: Start a sync producer
                type: boolean
              timeout:
                default: 10s
                description: Kafka producer timeout
                type: string
              tls:
                description: Kafka TLS configuration
                properties:
                  ca-file:
                    description: TLS Certificate Authority file path
                    type: string
                  cert-file:
                    description: TLS Certificate file path
                    type: string
                  key-file:
                    description: TLS Key file path
                    type: string
                  skip-verify:
                    description: Skip verifying the server certificate
                    type: boolean
                type: object
            required:
            - brokers
            - exports
            type: object
          status:
            description: ClusterProducerStatus defines the observed state of ClusterProducer
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
