---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: trapgroups.snmp.eda.nokia.com
spec:
  group: snmp.eda.nokia.com
  names:
    kind: TrapGroup
    listKind: TrapGroupList
    plural: trapgroups
    singular: trapgroup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TrapGroup is the Schema for the trapgroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TrapGroupSpec defines the desired state of TrapGroup
            properties:
              destinations:
                description: List of SNMP Destinations
                items:
                  type: string
                type: array
              names:
                description: List of SNMP trap names to be sent out.
                items:
                  type: string
                type: array
            type: object
          status:
            description: TrapGroupStatus defines the observed state of TrapGroup
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
