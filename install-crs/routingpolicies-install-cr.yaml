apiVersion: core.eda.nokia.com/v1
kind: Workflow
metadata:
  name: routingpolicies-nokia-install
  namespace: eda-system
spec:
  type: app-installer
  input:
    operation: install
    apps:
      - app: routingpolicies
        catalog: eda-catalog-builtin-apps
        vendor: nokia
        version:
          value: v4.0.2
          type: semver
