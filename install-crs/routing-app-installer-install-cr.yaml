apiVersion: appstore.eda.nokia.com/v1
kind: AppInstaller
metadata:
  name: routing-nokia-install
  namespace: eda-system
spec:
  operation: install
  apps:
    - appId: routing.eda.nokia.com
      catalog: eda-catalog-builtin-apps
      version:
        type: semver
        value: v5.0.2
