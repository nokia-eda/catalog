apiVersion: core.eda.nokia.com/v1
kind: Workflow
metadata:
  name: filters-nokia-install
  namespace: eda-system
spec:
  type: app-installer
  input:
    operation: install
    apps:
      - app: filters
        catalog: eda-catalog-builtin-apps
        vendor: nokia
        version:
          value: v5.0.1
          type: semver
