apiVersion: appstore.eda.nokia.com/v1
kind: AppInstaller
metadata:
  name: management-nokia-install
  namespace: eda-system
spec:
  operation: install
  apps:
    - appId: management.eda.nokia.com
      catalog: eda-catalog-builtin-apps
      version:
        type: semver
        value: v5.0.0
