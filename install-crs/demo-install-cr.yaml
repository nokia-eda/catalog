apiVersion: core.eda.nokia.com/v1
kind: Workflow
metadata:
  name: demo-nokia-install
  namespace: eda-system
spec:
  type: app-installer
  input:
    operation: install
    apps:
      - app: demo
        catalog: eda-catalog-builtin-apps
        vendor: nokia
        version:
          value: v5.0.0
          type: semver
