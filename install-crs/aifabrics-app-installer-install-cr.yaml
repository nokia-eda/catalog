apiVersion: appstore.eda.nokia.com/v1
kind: AppInstaller
metadata:
  name: aifabrics-nokia-install
  namespace: eda-system
spec:
  operation: install
  apps:
    - appId: aifabrics.eda.nokia.com
      catalog: eda-catalog-builtin-apps
      version:
        type: semver
        value: v5.0.2
