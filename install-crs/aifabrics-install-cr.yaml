apiVersion: core.eda.nokia.com/v1
kind: Workflow
metadata:
  name: aifabrics-nokia-install
  namespace: eda-system
spec:
  type: app-installer
  input:
    operation: install
    apps:
      - app: aifabrics
        catalog: eda-catalog-builtin-apps
        vendor: nokia
        version:
          value: v5.0.2
          type: semver
