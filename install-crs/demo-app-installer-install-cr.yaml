apiVersion: appstore.eda.nokia.com/v1
kind: AppInstaller
metadata:
  name: demo-nokia-install
  namespace: eda-system
spec:
  operation: install
  apps:
    - appId: demo.eda.nokia.com
      catalog: eda-catalog-builtin-apps
      version:
        type: semver
        value: v5.0.1
