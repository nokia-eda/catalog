apiVersion: appstore.eda.nokia.com/v1
kind: AppInstaller
metadata:
  name: config-nokia-install
  namespace: eda-system
spec:
  operation: install
  apps:
    - appId: config.eda.nokia.com
      catalog: eda-catalog-builtin-apps
      version:
        type: semver
        value: v5.0.1
